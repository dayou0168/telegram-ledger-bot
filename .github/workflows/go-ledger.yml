name: Go Ledger Runtime

on:
  push:
    branches:
      - main
      - master
    paths:
      - "go-ledger/**"
      - ".github/workflows/go-ledger.yml"
  pull_request:
    paths:
      - "go-ledger/**"
      - ".github/workflows/go-ledger.yml"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: ledger_bot_test
          POSTGRES_USER: ledger
          POSTGRES_PASSWORD: ledger
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ledger -d ledger_bot_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Download dependencies
        working-directory: go-ledger
        run: go mod download

      - name: Test
        working-directory: go-ledger
        env:
          TEST_DATABASE_URL: postgres://ledger:ledger@localhost:5432/ledger_bot_test?sslmode=disable
        run: go test -count=1 ./...

      - name: Vet
        working-directory: go-ledger
        run: go vet ./...

      - name: Build
        working-directory: go-ledger
        run: |
          go build ./cmd/ledger-bot
          go build ./cmd/chain-watcher

      - name: Build host chain watcher package
        working-directory: go-ledger
        run: |
          set -euo pipefail
          rm -rf dist
          mkdir -p dist/ledger-chain-watcher-v2.4.0-linux-amd64
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o dist/ledger-chain-watcher-linux-amd64 ./cmd/chain-watcher
          cp dist/ledger-chain-watcher-linux-amd64 dist/ledger-chain-watcher-v2.4.0-linux-amd64/ledger-chain-watcher
          cp ../deploy/ledger-chain-watcher.env.example dist/ledger-chain-watcher-v2.4.0-linux-amd64/
          cp ../deploy/ledger-chain-watcher.service dist/ledger-chain-watcher-v2.4.0-linux-amd64/
          tar -C dist -czf dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz ledger-chain-watcher-v2.4.0-linux-amd64
          sha256sum dist/ledger-chain-watcher-linux-amd64 > dist/ledger-chain-watcher-linux-amd64.sha256
          sha256sum dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz > dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz.sha256

      - name: Upload host chain watcher artifact
        uses: actions/upload-artifact@v4
        with:
          name: ledger-chain-watcher-v2.4.0-linux-amd64
          path: |
            go-ledger/dist/ledger-chain-watcher-linux-amd64
            go-ledger/dist/ledger-chain-watcher-linux-amd64.sha256
            go-ledger/dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz
            go-ledger/dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz.sha256

      - name: Build Docker images
        working-directory: go-ledger
        run: |
          docker build --build-arg APP=ledger-bot -t telegram-ledger-bot-go:2.4.0 .
          docker build --build-arg APP=chain-watcher -t telegram-ledger-chain-watcher:2.4.0 .

      - name: Login to GHCR
        if: github.event_name == 'push'
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Publish Docker image
        if: github.event_name == 'push'
        working-directory: go-ledger
        run: |
          BOT_IMAGE="ghcr.io/${{ github.repository_owner }}/telegram-ledger-bot-go"
          WATCHER_IMAGE="ghcr.io/${{ github.repository_owner }}/telegram-ledger-chain-watcher"
          docker tag telegram-ledger-bot-go:2.4.0 "$BOT_IMAGE:2.4.0"
          docker tag telegram-ledger-bot-go:2.4.0 "$BOT_IMAGE:latest"
          docker tag telegram-ledger-chain-watcher:2.4.0 "$WATCHER_IMAGE:2.4.0"
          docker tag telegram-ledger-chain-watcher:2.4.0 "$WATCHER_IMAGE:latest"
          docker push "$BOT_IMAGE:2.4.0"
          docker push "$BOT_IMAGE:latest"
          docker push "$WATCHER_IMAGE:2.4.0"
          docker push "$WATCHER_IMAGE:latest"

      - name: Publish host chain watcher release asset
        if: github.event_name == 'push'
        working-directory: go-ledger
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          cat > /tmp/release-notes.md <<'EOF'
          Go v2.4.0 upgrades the shared TRON watcher architecture and must be deployed to both the bot and ledger-chain-watcher.

          Before upgrading:
          - Back up the bot and watcher PostgreSQL databases.
          - Set CHAIN_WATCHER_KEY_ENCRYPTION_KEY to a securely generated base64-encoded 32-byte key.
          - Set a unique, stable BOT_FALLBACK_INSTANCE_ID for every bot instance.
          - Configure BOT_FALLBACK_SHARED_DATABASE_URL for the shared PostgreSQL lease and recovery watermark.

          CI runs the full Go suite against PostgreSQL 16, go vet, and both bot/watcher builds. Real Tronscan 429 behavior, real transfers, and live leader failover remain deployment acceptance checks.
          EOF
          ASSETS=(
            "dist/ledger-chain-watcher-linux-amd64"
            "dist/ledger-chain-watcher-linux-amd64.sha256"
            "dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz"
            "dist/ledger-chain-watcher-v2.4.0-linux-amd64.tar.gz.sha256"
          )
          if gh release view v2.4.0 --repo "${{ github.repository }}" >/dev/null 2>&1; then
            gh release upload v2.4.0 "${ASSETS[@]}" --clobber --repo "${{ github.repository }}"
          else
            gh release create v2.4.0 "${ASSETS[@]}" \
              --target "${{ github.sha }}" \
              --title "Go v2.4.0" \
              --notes-file /tmp/release-notes.md \
              --latest \
              --repo "${{ github.repository }}"
          fi
